node {
    stage('git pull'){
        checkout scm
        def customImage = docker.build("ubuntu:latest", "./dockerfiles/")
        customImage.inside {
            sh 'touch /tmp/test1.txt'
        }
     }
}
