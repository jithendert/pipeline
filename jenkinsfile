node {
    stage('git pull'){
        checkout scm
        def customImage = docker.build("ngnix:latest", "./dockerfiles/")
        customImage.inside {
            sh 'touch /tmp/test1.txt'
        }
     }
}
