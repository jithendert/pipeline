node {
	def commitId
	
	stage('git pull') {
		checkout scm
	}
	stage('git read commit') {
		sh 'git rev-parse HEAD > commitid'
		commitId = readFile('commitid')
		println("commitid: " + commitId)
		println("last commit Id: " + ${env.GIT_PREVIOUS_SUCCESSFUL_COMMIT})
	}
}
